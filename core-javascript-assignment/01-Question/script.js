//01. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with their data type.

'use strict';
let varNumber = 1234;
let varString = "Hello world";
let varBoolean = true;
let varUndefined;
let varNull = null;

console.log(`The type of ${varNumber} variable is ${typeof varNumber}`);
console.log(`The type of ${varString} variable is ${typeof varString}`);
console.log(`The type of ${varBoolean} variable is ${typeof varBoolean}`);
console.log(`The type of ${varUndefined} variable is ${typeof varUndefined}`);
console.log(`The type of ${varNull} variable is ${typeof varNull}`);