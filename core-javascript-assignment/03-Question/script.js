// 03. Declare a varibale and assign string value to it and change all the string characters to capital letters using toUpperCase() method.

'use strict';

const varLowercaseString = "Hey people I'm learning javascript";
console.log(`convert "${varLowercaseString}" into uppercase String`);
const upperCaseStringIs = varLowercaseString.toUpperCase();
console.log(`Uppercase string is "${upperCaseStringIs}"`);